steps:
- name: 'ubuntu'
  args: ['echo', 'hello world']

# steps:
# # Download a sample data file (could have been generated by a binary).
# - name: 'gcr.io/cloud-builders/wget'
#   args: ['https://storage.cloud.google.com/gwilearningresources/antonis_test/test_data2.json']
# # Load it into a BigQuery table
# - name: 'gcr.io/community-builders/bq'
#   args: ['load', '--replace', 'data-cloud-363308.dev_antonis.test_imported_data2', 'test_data2.json',
#          'name:string,post_abbr:string']
